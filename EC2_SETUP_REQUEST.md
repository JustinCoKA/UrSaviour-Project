# EC2インスタンス作成依頼 - UrSaviourプロジェクト

## 📋 EC2インスタンス作成仕様

### **基本設定**
- **プロジェクト名**: UrSaviour
- **用途**: Webアプリケーション（Python FastAPI + MySQL + nginx）
- **予想トラフィック**: 開発・テスト用

### **推奨スペック**
- **AMI**: Ubuntu 22.04 LTS (Free Tier eligible)
- **インスタンスタイプ**: t3.micro (無料枠) または t3.small
- **ストレージ**: 20GB gp3 (汎用SSD)
- **キーペア**: 新規作成または既存使用

### **セキュリティグループ設定**
以下のポートを開放してください：

| プロトコル | ポート | ソース | 用途 |
|-----------|--------|--------|------|
| SSH | 22 | My IP | SSH接続 |
| HTTP | 80 | 0.0.0.0/0 | Webアプリ |
| HTTPS | 443 | 0.0.0.0/0 | SSL (将来用) |
| Custom TCP | 8000 | 0.0.0.0/0 | API (開発用) |

### **必要な情報**
作成後、以下の情報を教えてください：
1. **パブリックIP**: xxx.xxx.xxx.xxx
2. **SSH接続コマンド**: `ssh -i key.pem ubuntu@xxx.xxx.xxx.xxx`
3. **キーペアファイル**: key.pemファイルのダウンロード

### **タグ設定（推奨）**
- **Name**: UrSaviour-Web-Server
- **Project**: UrSaviour
- **Environment**: Development
- **Owner**: [リーダー名]

### **今後の予定**
インスタンス作成後、以下を実行予定：
1. 自動デプロイスクリプトの実行
2. アプリケーションのテスト
3. ドメイン設定（必要に応じて）

---

## 🎯 簡単作成手順（リーダー向け）

1. **EC2コンソール** → **インスタンスを起動**
2. **Ubuntu 22.04 LTS** を選択
3. **t3.micro** を選択
4. **新しいキーペア作成** または既存選択
5. **セキュリティグループ** で上記ポートを設定
6. **起動** ボタンをクリック
7. **パブリックIP** をチーム共有

---

**予算**: t3.microなら月額 約$10-15 (750時間無料枠あり)